export const environment = {
  production: false,
  // Use the exact port/scheme your backend prints on startup:
  // e.g. "Now listening on: https://localhost:5001; http://localhost:5000"
  apiBase: 'http://localhost:5000/api'
};
